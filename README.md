# typescript-learn

A small TypeScript learning project.

## Getting started

Install dependencies:

```sh
npm install
```

Run the type checker:

```sh
npm run check
```

Build JavaScript into `dist/`:

```sh
npm run build
```

Watch for changes:

```sh
npm run dev
```